apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-07
  namespace: qa-challenge-07
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-07
  template:
    metadata:
      labels:
        app: web-07
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 3
            failureThreshold: 2
